{
  "name": "petstore-api-tests",
  "version": "1.0.0",
  "description": "API test automation suite for Swagger Petstore",
  "scripts": {
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000,
    "verbose": true
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}